import { HeroSection } from './HeroSection'

/**
 * @deprecated Use {@link HeroSection} or {@link HeroWrapper} directly.
 * This thin wrapper exists for backward compatibility with legacy imports.
 */
export const OptimizedHeroSection = HeroSection
